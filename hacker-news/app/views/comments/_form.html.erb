<%= form_for [@post, @comment] do |f| %>
  <fieldset>
    <legend>Post Details: </legend>
  <% [:body].each do |field| %>
  <div>
    <%= f.label field %>
    <%= f.text_field field %>
  </div>
  <% end %>
    <%= f.submit %>
  </fieldset>
<% end %>
